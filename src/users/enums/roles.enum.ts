/**
 * Defines the possible roles for users in the application.
 * This enum is used to specify the roles that can be assigned to users and to control access to certain routes based on these roles.
 */
export enum Roles {
  ADMIN = 'admin',
  USER = 'user',
}
